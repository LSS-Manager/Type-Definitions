to create definitions file, rename application.js into application.ts temporarily.
Then run the following command in root directory of project:
tsc src/application/application.ts --declaration --outDir src/application/

afterwards run the eslint-command in same directory:

eslint src/application/application.d.ts --fix

afterwards rename application.ts back to application.js
